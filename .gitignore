# GitHub profile repo (jamesbond379/jamesbond379).
# This folder IS the repo root — README.md renders on the profile page.
# Only profile assets live here; the FitzJobs pipeline is in the parent folder,
# which is NOT part of this repo.

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
